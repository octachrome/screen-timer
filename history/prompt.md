you are implementing phase 1 of the plan in plan.md. create a detailed todo list for this phase in todo.md, then pick the 10 most important tasks to work on first, and delegate them to sub-agents. every time you complete a task, run the tests and make sure they pass. if you change client files, run the client tests; if you change server files, run the server tests. then update the todo list and commit your changes before moving on to the next task. whenever you find another task that needs to be done, if it fits into the theme of the current phase, add it to the todo list.
