you are implementing the MVP defined in mvp.md. implement the items in the todo list in todo.md. pick the 10 most important tasks to work on first, and delegate them to sub-agents. every time you complete a task, run the tests and make sure they pass. if you change client files, run the client tests; if you change server files, run the server tests. then update the todo list and commit your changes before moving on to the next task. whenever you find another task that needs to be done, add it to the todo list.
